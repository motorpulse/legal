import { naturalDate } from '@/utils/misc';

/**
 * Date of update for host/privacy-policy
 */
const privacyPolicyUpdatedAt = () => {
  return naturalDate(12, 8, 2025);
};

export default privacyPolicyUpdatedAt;
